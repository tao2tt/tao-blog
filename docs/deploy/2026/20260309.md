# 2026-03-09 上线记录

## 上线信息
- **上线日期**：2026-03-09
- **上线环境**：生产环境
- **需求数量**：1 个

---

## 需求清单

| 序号 | 需求名称 | 涉及服务 | 变更类型 |
|------|----------|----------|----------|
| 1 | 神南产业 N+2 推送项目接口字段调整 | api_web、数据库 | 修改 |

---

## 详细变更

### 1. 神南产业 N+2 推送项目接口字段调整

**涉及服务：**
- `api_web`
- 数据库 `platform_user`

**需求描述：**
神南产业 N+2 推送项目接口中，项目名称字段最大值调整为 300

**变更内容：**

#### 数据库变更
- **数据库**：`platform_user`
- **表名**：`project`
- **字段**：`name`
- **变更**：`varchar(100)` → `varchar(300)`

#### 接口变更
- **服务**：`api_web`
- **接口**：N+2 推送项目接口
- **变更**：项目名称字段长度校验调整为 300

---

## 涉及服务汇总

| 服务名称 | 变更内容 | 备注 |
|----------|----------|------|
| api_web | N+2 推送项目接口字段长度调整 | 字段长度 100→300 |
| platform_user 库 | project 表 name 字段类型调整 | varchar(100)→varchar(300) |

---

## SQL 脚本

```sql
-- 修改 project 表 name 字段长度
ALTER TABLE `platform_user`.`project` 
MODIFY COLUMN `name` varchar(300) DEFAULT NULL COMMENT '项目名称';
```

---

## Nacos 配置变更

> 本次上线无配置变更

---

## 验证结果

- [ ] 功能验证待补充
- [ ] 性能验证待补充
- [ ] 监控正常待补充

---

## 回滚方案

```sql
-- 如需回滚，执行以下 SQL
ALTER TABLE `platform_user`.`project` 
MODIFY COLUMN `name` varchar(100) DEFAULT NULL COMMENT '项目名称';
```

---

**记录人**：涛哥  
**记录时间**：2026-03-11
